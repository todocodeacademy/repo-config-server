server:
  port: 8081

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/todocodeacademy/config-server-test.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}

